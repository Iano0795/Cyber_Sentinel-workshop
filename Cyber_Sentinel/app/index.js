import {View, Text, SafeAreaView, ScrollView} from 'react-native';

const app = () => {
    return (
        <SafeAreaView>
            <ScrollView>
            <Text>CYBER SENTINEL WORKSHOP</Text>
            </ScrollView>
        </SafeAreaView>
    );
}